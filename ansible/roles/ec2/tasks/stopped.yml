---

- name: Stop all 'performance' tagged EC2 Instances
  amazon.aws.ec2:
    region: us-west-1
    instance_tags:
        Name: scale
        purpose: performance
    state: stopped
  register: ec2

- name: Stopped EC2 instances
  debug:
    msg: 
      - "{{ ec2 }}"
